# Server port
PORT=8080

# Comma-separated list of IP addresses and CIDR ranges that are whitelisted
# Requests from these IPs will bypass authentication
IP_WHITELIST=127.0.0.1,::1,192.168.1.0/24

# OIDC token audience (should match your proxy URL)
AUDIENCE=https://goproxy.example.com

# Backend Go module proxy URL
GOPROXY_URL=https://proxy.golang.org
